<odoo>
    <data>
        <record model="test_model" id="openacademy.nombres">
            <field name="name">Pepe</field> <!-- El nombre debe ser el mismo que la variable del modelo -->
            <field name="description">50</field> <!-- El nombre debe ser el mismo que la variable del modelo -->
        </record>

        <record model="puntuacion" id="openacademy.usuarios">
            <field name="usuario">Gaymer</field> <!-- El nombre debe ser el mismo que la variable del modelo -->
            <field name="puntos">100</field> <!-- El nombre debe ser el mismo que la variable del modelo -->
        </record>
    </data>
</odoo>